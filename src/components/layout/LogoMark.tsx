import type { ReactNode } from 'react'

/**
 * The official portrait mark has not been supplied. Rendering nothing is intentional:
 * fabricating a monogram or portrait would violate the brand brief.
 */
export function LogoMark(): ReactNode {
  return null
}
